using UnityEngine;
using System.Collections;

public class DestroyOnLoad : MonoBehaviour {

	public float desTimer = 1f;
	
	// Use this for initialization
	void Start () {
		Destroy(gameObject, desTimer);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
